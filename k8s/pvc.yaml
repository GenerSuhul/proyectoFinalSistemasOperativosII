apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oracle-data
  namespace: airport
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
